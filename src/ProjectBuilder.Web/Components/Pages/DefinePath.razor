@page "/projects/{ProjectId}/paths/new"

<PageTitle>Define recovery · Project Builder</PageTitle>
<main class="product-page path-studio-page">
    <nav aria-label="Breadcrumb"><a href="/projects/@ProjectId">Project overview</a> <span aria-hidden="true">/</span> Define branch and recovery</nav>
    <ProjectBuilder.Web.Client.Components.PathRecoveryStudio ProjectId="@ProjectId" @rendermode="@(new InteractiveWebAssemblyRenderMode(prerender: false))" />
</main>

@code { [Parameter] public string ProjectId { get; set; } = string.Empty; }
