@page "/projects/{ProjectId}/state-logic/new"

<PageTitle>State and logic studio · Project Builder</PageTitle>
<main class="product-page state-studio-page">
    <nav aria-label="Breadcrumb"><a href="/projects/@ProjectId">Project overview</a> <span aria-hidden="true">/</span> State and logic</nav>
    <ProjectBuilder.Web.Client.Components.StateLogicStudio ProjectId="@ProjectId" @rendermode="@(new InteractiveWebAssemblyRenderMode(prerender: false))" />
</main>

@code { [Parameter] public string ProjectId { get; set; } = string.Empty; }
