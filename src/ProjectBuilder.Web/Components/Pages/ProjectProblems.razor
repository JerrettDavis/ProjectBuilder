@page "/projects/{ProjectId}/problems"

<PageTitle>Purpose and gap map · Project Builder</PageTitle>
<main class="product-page problems-page">
    <nav aria-label="Breadcrumb"><a href="/projects/@ProjectId">Project overview</a> <span aria-hidden="true">/</span> Purpose and gap map</nav>
    <ProjectBuilder.Web.Client.Components.ProblemsWorkbench ProjectId="@ProjectId" @rendermode="@(new InteractiveWebAssemblyRenderMode(prerender: false))" />
</main>

@code { [Parameter] public string ProjectId { get; set; } = string.Empty; }
