# Diagram Sources

These Mermaid sources are standalone versions of the core diagrams embedded throughout the documentation.

| File | Shows |
|---|---|
| `product-loop.mmd` | product definition from actor outcome through learning |
| `model-hierarchy.mmd` | containment hierarchy plus semantic cross-links |
| `system-context.mmd` | Project Builder users and external systems |
| `container-architecture.mmd` | initial modular-monolith containers |
| `write-path.mmd` | draft, validation, atomic commit, and notification |
| `delivery-loop.mmd` | Definition-Validated Delivery |
| `state-separation.mmd` | domain, workflow, target UI, infrastructure, and canvas state |
| `interface-drilldown.mmd` | progressive decomposition of a POS scan |
| `collaboration-conflict.mmd` | optimistic concurrency and operation rebase |
| `agent-proposal.mmd` | policy-controlled uncommitted agent proposal |
| `roadmap.mmd` | product horizons |

Mermaid sources are reviewable text. Rendered images should be generated deterministically in CI when the repository adds a documentation build. Do not hand-edit rendered outputs.
